# Moved out of dotfiles.
# The live Codex config now lives in the private repo and is linked into ~/.codex/config.toml.
# Run ~/my_src/private/descript_update.sh to resync the private Codex skill and config wiring.
